# Copy to .env and fill in real values.
# For local development, docker-compose.yml spins up a Postgres container
# so the value below matches it out of the box.
DATABASE_URL="postgresql://portfolio_user:portfolio_pass@localhost:5432/portfolio"
