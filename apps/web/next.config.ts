import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Images served from public/ don't need extra config.
  // Add remote domains here if you ever serve images from an external URL.
};

export default nextConfig;
